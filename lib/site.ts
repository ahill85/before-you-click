/** Canonical public URL (astar). Vercel still hosts the app behind a proxy. */
export const SITE_URL = "https://astarmedia.net/before-you-click";

/** Vercel origin — used by the astar reverse-proxy target. */
export const VERCEL_ORIGIN = "https://before-you-click.vercel.app";
